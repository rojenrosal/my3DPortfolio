import type { Metadata } from "next";
import "./globals.css";
import { Navbar } from "@/components/navbar";
import { Footer } from "@/components/footer";

export const metadata: Metadata = {
  title: "Hireloop - Job Board for Product &amp; Engineering Teams (Demo)",
  description:
    "Demo project: a modern job board with browse, post, and employer dashboard flows. Built with Next.js, TypeScript, Tailwind, and shadcn/ui.",
  robots: { index: false, follow: false },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className="min-h-screen antialiased">
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
